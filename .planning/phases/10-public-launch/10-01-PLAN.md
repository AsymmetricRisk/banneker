---
phase: 10-public-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - package.json
autonomous: true

must_haves:
  truths:
    - "README.md exists with installation instructions"
    - "README.md includes quick start guide"
    - "README.md includes command reference"
    - "package.json has complete metadata for npm registry"
    - "npm pack --dry-run shows expected files without sensitive data"
  artifacts:
    - path: "README.md"
      provides: "Package documentation for npm registry"
      min_lines: 50
      contains: "npx banneker"
    - path: "package.json"
      provides: "Complete npm package metadata"
      contains: "repository"
  key_links:
    - from: "package.json"
      to: "README.md"
      via: "npm registry display"
      pattern: "homepage.*github"
---

<objective>
Prepare Banneker package for npm publication by creating comprehensive README documentation and adding required package.json metadata.

Purpose: npm packages without README appear broken on the registry. Missing metadata (repository, homepage, keywords) reduces discoverability and user trust. This plan ensures the published package presents professionally.

Output: Complete README.md with installation, quick start, and command reference. Enhanced package.json with all npm registry metadata fields.
</objective>

<execution_context>
@/home/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/home/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-public-launch/10-RESEARCH.md
@package.json
@bin/banneker.js
@templates/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive README.md</name>
  <files>README.md</files>
  <action>
Create README.md with the following sections:

1. **Header with badges:**
   - npm version badge: `[![npm version](https://badge.fury.io/js/banneker.svg)](https://www.npmjs.com/package/banneker)`
   - CI status badge (GitHub Actions)
   - License badge (MIT)

2. **Description:** One-liner matching package.json description, then expanded paragraph explaining what Banneker does (project planning pipeline for AI coding assistants).

3. **Installation section:**
   - Primary: `npx banneker` (no install required)
   - Global: `npm install -g banneker`
   - Runtime selection flags: `--claude`, `--opencode`, `--gemini`
   - Location flags: `--global`, `--local`

4. **Quick Start section:**
   - Step 1: Run installer with runtime selection
   - Step 2: Run survey command in project directory
   - Step 3: Generate documents and diagrams
   - Step 4: Export to framework format

5. **Command Reference table:**
   | Command | Description |
   | `/banneker:survey` | Conduct 6-phase discovery interview |
   | `/banneker:architect` | Generate planning documents |
   | `/banneker:roadmap` | Generate architecture diagrams |
   | `/banneker:appendix` | Compile HTML reference |
   | `/banneker:feed` | Export to framework format |
   | `/banneker:document` | Analyze existing codebase |
   | `/banneker:audit` | Evaluate plans against rubric |
   | `/banneker:plat` | Generate sitemap/route architecture |
   | `/banneker:progress` | Show current Banneker state |
   | `/banneker:help` | Display command reference |

6. **Requirements section:**
   - Node.js >= 18.0.0
   - AI coding assistant (Claude Code, OpenCode, or Gemini)
   - Zero runtime dependencies

7. **Supported Runtimes section:**
   - Claude Code (primary)
   - OpenCode
   - Gemini

8. **Security section:**
   - Brief mention of file-write surface (installer writes to ~/.claude/)
   - Link to SECURITY.md for threat model
   - Mention of npm provenance attestation

9. **License section:** MIT with link to LICENSE file

10. **Links section:**
    - npm package URL
    - GitHub repository URL
    - Issue tracker URL

Do NOT include:
- Contributing guidelines (no CONTRIBUTING.md exists)
- Changelog section (auto-generated separately)
- Detailed API documentation (commands are self-documenting)
  </action>
  <verify>
- `test -f README.md` returns success
- `grep -c "npx banneker" README.md` returns at least 2 (installation + quick start)
- `grep "Command Reference" README.md` shows section exists
- `grep "Installation" README.md` shows section exists
- `wc -l README.md` shows at least 50 lines
  </verify>
  <done>README.md exists with installation instructions, quick start guide, and command reference for all Banneker commands. File is ready for npm registry display.</done>
</task>

<task type="auto">
  <name>Task 2: Add package.json npm registry metadata</name>
  <files>package.json</files>
  <action>
Update package.json to add missing fields required for professional npm registry presence:

1. **repository field:**
```json
"repository": {
  "type": "git",
  "url": "git+https://github.com/dsj7419/banneker.git"
}
```

2. **homepage field:**
```json
"homepage": "https://github.com/dsj7419/banneker#readme"
```

3. **bugs field:**
```json
"bugs": {
  "url": "https://github.com/dsj7419/banneker/issues"
}
```

4. **keywords array** (for npm search discoverability):
```json
"keywords": [
  "project-planning",
  "documentation",
  "ai-coding-assistant",
  "claude-code",
  "opencode",
  "gemini",
  "cli",
  "developer-tools",
  "architecture-diagrams",
  "survey"
]
```

5. **author field:**
```json
"author": "Daniel Jackson"
```

Note: Derive GitHub username from existing .git config if available, otherwise use placeholder that user can update. Check `git remote -v` to find the actual repository URL.

Preserve all existing fields (name, version, description, type, bin, engines, license, files, scripts, devDependencies).
  </action>
  <verify>
- `jq '.repository' package.json` returns object with url containing "github.com"
- `jq '.homepage' package.json` returns URL string
- `jq '.bugs' package.json` returns object with url field
- `jq '.keywords | length' package.json` returns at least 5
- `jq '.author' package.json` returns non-null value
- `npm pack --dry-run` succeeds without errors
  </verify>
  <done>package.json contains repository, homepage, bugs, keywords, and author fields. npm registry will display proper metadata and links.</done>
</task>

<task type="auto">
  <name>Task 3: Run pre-publish verification</name>
  <files>None (verification only)</files>
  <action>
Execute pre-publish verification steps to ensure package is ready for npm:

1. **Version sync check:**
   - Verify `jq -r '.version' package.json` matches `cat VERSION`
   - Both should show "0.2.0"

2. **Test suite verification:**
   - Run `npm test` and confirm all tests pass
   - Note: Tests may fail in CI if environment not fully set up, but should pass locally

3. **Package contents verification:**
   - Run `npm pack --dry-run` and review output
   - Expected files: bin/banneker.js, lib/*, templates/*, VERSION, README.md
   - Verify NO sensitive files: .env*, credentials*, secrets*, .git/

4. **Metadata verification:**
   - Run `npm pkg get name version description repository homepage bugs keywords author`
   - Confirm all fields populated

5. **README inclusion check:**
   - Verify `npm pack --dry-run | grep README.md` shows README will be included

6. **Executable verification:**
   - Verify `ls -la bin/banneker.js` shows executable permission (x bit)
   - Verify `head -1 bin/banneker.js` shows shebang line `#!/usr/bin/env node`

Report any issues found. All checks must pass before proceeding to publish.
  </action>
  <verify>
All verification commands complete without errors:
- Version sync confirmed
- Test suite passes
- No sensitive files in pack output
- All metadata fields present
- README.md included
- bin/banneker.js has shebang and executable permission
  </verify>
  <done>Pre-publish verification complete. Package is ready for npm publish. All files, metadata, and permissions verified correct.</done>
</task>

</tasks>

<verification>
1. README.md exists and contains required sections (install, quick start, commands)
2. package.json has complete npm registry metadata
3. `npm pack --dry-run` shows clean package contents
4. No sensitive files will be published
5. Version is 0.2.0 in both package.json and VERSION
</verification>

<success_criteria>
- README.md created with 50+ lines containing installation, quick start, and command reference
- package.json enhanced with repository, homepage, bugs, keywords, author fields
- `npm pack --dry-run` confirms package contents are correct
- Pre-publish verification confirms package is ready for npm registry
</success_criteria>

<output>
After completion, create `.planning/phases/10-public-launch/10-01-SUMMARY.md`
</output>
